---
- name: 'Update apt cache...'
  when: ansible_pkg_mgr == 'apt'
  become: '{{ cli_tools_privilege_escalation }}'
  become_user: root
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: 'Install CLI tools os pkgs...'
  become: '{{ cli_tools_privilege_escalation }}'
  become_user: root
  ansible.builtin.package:
    name: '{{ cli_tools_os_pkgs }}'
    state: present
