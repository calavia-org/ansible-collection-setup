---
- name: 'Resolve platform specific vars'
  ansible.builtin.include_vars: '{{ item }}'
  with_first_found:
    - files:
        - '{{ ansible_distribution }}-{{ ansible_distribution_release }}.yml'
        - '{{ ansible_distribution }}.yml'
        - '{{ ansible_os_family }}.yml'
      skip: true
      paths:
        - '{{ role_path }}/vars'

- name: 'Install OpenCode'
  ansible.builtin.include_tasks: '{{ opencode_installer }}.yml'

- name: 'Install plugin dependencies'
  ansible.builtin.include_tasks: install_deps.yml

- name: 'Include plugin roles'
  ansible.builtin.include_role:
    name: "{{ item.role }}"
  loop: "{{ opencode_plugin_roles }}"
  loop_control:
    loop_var: item
  when: item.plugin in (opencode_plugins | map(attribute='name'))

- name: 'Configure OpenCode'
  ansible.builtin.include_tasks: configure.yml
  when: not opencode_skip_configure | bool
